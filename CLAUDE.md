# CLAUDE.md — 手取りのホント(仮) プロジェクト規約

このファイルは、このリポジトリで作業する Claude Code に向けた恒久的な指示です。
セッションが変わっても、この内容は常に有効です。

## 0. 最初に必ずやること

作業に着手する前に、必ず `SPEC.md` を読むこと。SPEC.md はこのサイトの仕様の唯一の正とする文書であり、要件・ページ構成・入力項目・スコープ外・完了条件はすべてここに定義されている。SPEC.md を読まずにコードを書き始めてはならない。

会話の最初の指示が実装依頼であっても、まず SPEC.md を読み、その内容を踏まえた**実装計画**を提示し、ユーザーの承認を得てから着手すること。計画なしにファイルを作成・編集し始めてはならない。

## 1. 進行ルール(この順序を厳守する)

1. SPEC.md を読む
2. 着手する範囲(今回のセッションで何をどこまでやるか)を明確にする
3. 実装計画を提示する(変更するファイル、追加するファイル、処理の流れ、確認したい前提を含める)
4. ユーザーの承認を得る
5. 承認された計画どおりに実装する
6. 実装後、変更点を要約して報告する

推測で仕様を補わない。SPEC.md に書かれていないことで実装上の判断が必要な場合は、ユーザーに確認する。

## 2. スコープ厳守

SPEC.md の「6. スコープ外」に明記されている項目は実装しない。良かれと思って先回りで作らないこと(例: 保育料計算、副業・譲渡所得の反映、経費率スライダーをかんたん入力に混ぜる、など)。

「ついでにこれも」という提案自体はしてよいが、実装は必ずユーザーの承認後にする。

## 3. 変更要望が出た場合

実装中や実装後に、SPEC.md に書かれていない変更・追加の要望が出た場合、いきなりコードに反映しない。次の順序を踏む。

1. SPEC.md の該当箇所をどう変更するかの差分案を提示する
2. ユーザーの確認を得る
3. SPEC.md を更新する(バージョン番号と変更履歴を必ず更新)
4. 更新後の SPEC.md に基づいて実装する

SPEC.md の「変更履歴」テーブルは、行を追加する形で更新し、過去の版の記述を書き換えたり消したりしない。

## 4. リリース方針(同時設計・段階リリース)

このプロジェクトは「かんたん入力(第1弾)」「詳細入力(第2弾)」の2段階リリースを前提にしている。

- 計算エンジン・データ構造・型定義は、最初から詳細入力モードの項目(iDeCo、生命保険料、医療費、住宅ローン控除、経費率、青色申告、ボーナス比率など)を見据えて設計すること。あとから詳細モードを継ぎ足すのではなく、最初から両対応の構造にする。
- ただし UI 上の公開は段階的。第1弾では詳細入力タブは「準備中」として非表示またはグレーアウトし、かんたん入力のみで完結させる。
- 「今回は第1弾のどこまでを実装するか」は、その都度の実装計画で明示すること。

## 5. データと計算に関する注意

- 国保料率・税率・控除額などの数値は、必ず SPEC.md に記載された一次ソース(国税庁、厚労省、都道府県公表資料など)に基づくこと。数値を推測・仮定で埋めない。
- 未整備の市区町村データは、平均値や近隣自治体の値で代替しない。「データ未整備」として明示する(SPEC.md 5章の方針どおり)。
- 計算結果には常に「概算である」旨、出典、対象年度を添えること。
- YMYL(税務・お金)領域であることを常に意識し、断定的な表現、個別の税務相談に該当しうる表現を避ける。

## 6. Git 運用

- コミットメッセージは `git commit -F` で作成する(ヒアドキュメントでの直接指定は避ける)
- push 前に必ず `git fetch` を行い、origin/main との差分を確認する
- 生成ファイル(市区町村別ページなど自動生成物)がコンフリクトした場合: origin/main の生成ファイルを一旦ベースにし、生成スクリプトを再実行してから、生成ファイルには `ours` 戦略でマージし、手動のコード変更は個別に保持する
- 作業ツリーに未コミットの WIP がある状態で別件の変更を扱う場合は、`git apply --check` で影響範囲を確認してから `git stash` でパッチ分離する

## 7. 完了報告の基準

「完了しました」は次の3点をすべて確認してから報告すること。1つでも欠けた状態で完了と報告しない。

1. push が成功している
2. GitHub Actions のワークフローが成功している
3. 本番の GitHub Pages で実際に変更が反映されていることを確認している

## 8. 権限について

破壊的な操作(force push、履歴の書き換え、生成データの一括削除など)は、実行前に必ずユーザーに確認する。読み取り専用の調査(ファイル閲覧、grep、ビルド確認)は都度の確認なしに進めてよい。

## 8.5 Claude APIキーが必要な作業の環境切り分け(2026-08-05〜)

Claude Code on the Web(クラウド環境)には専用のシークレットストアが無く、環境変数欄に入れた値は暗号化されず誰でも読める設定として残り続ける。そのため `ANTHROPIC_API_KEY` のようなAPIキーをこのクラウド環境の環境変数欄には設定しない方針とした(ユーザー判断、2026-08-05)。

- `scripts/kokuho/`・`scripts/kyokai-kenpo/` など、`ANTHROPIC_API_KEY` を使ってClaude APIを直接呼び出すスクリプトの実行は、ユーザーのローカル環境(git bash)で行う。
- コード閲覧・実装計画の提示・SPEC.mdの更新・テスト実行など、Claude APIキーを必要としない作業は、このクラウド環境で行ってよい。
- **このクラウド環境のセッションで、上記のAPIキーが必要な作業に着手しそうになった場合は、実装や実行に進む前に「これはGit Bashで作業してください」とユーザーに告知し、作業を止めること。** 黙って環境変数欄にキーを設定しようとしたり、キー入力を求めたりしない。

## 9. 引き継ぎ事項(未確定・要確認)

- 住民税の調整控除における人的控除差は、2026-07-20の調査で**配偶者控除・扶養控除等は総務省一次資料(税制調査会説明資料、令和7年5月15日、cao.go.jp/zei-cho)で確定済み**(`src/data/rates/2026/resident-tax-standard.json` の `adjustmentCredit.personalDeductionDifferences`)。**基礎控除分のみ未確定**のまま(0円扱い)。所得税側の基礎控除が令和7・8年度改正で所得金額に応じた変動額(104万/67万/62万等)になったため、住民税43万円との単純な差額が一律に定義できないことが未確定の理由。
  - e-Gov法令検索(地方税法第314条の6)で条文を直接確認しようとしたが、ページがJavaScriptで描画される作りのため、ブラウザ操作ができない環境ではWebFetchで本文を取得できなかった。**次回、ブラウザ操作(Claude in Chrome等)が使える状態で同条文を確認するか、市販の地方税コンメンタールを参照すること。**
  - **重要な限界**: 住民税の調整控除は、総務省・国税庁等の公式計算例との照合ができておらず、自分たちで手計算した検算(`test/calc/resident-tax.test.mjs`)による自己整合性の確認のみで、外部の正解データとの突合は未実施。基礎控除の人的控除差が確定した際は、この手計算設例の期待値も合わせて更新すること。
- 配偶者特別控除の所得区分表(58万円超〜133万円以下、`spousal-deduction.json` の `spousalSpecialDeduction.brackets`)は、令和8年度改正で配偶者の所得要件が58万円→62万円に引き上げられた影響を受けている可能性が高いが、上限133万円側や区分ごとの金額が追随して変わったかは未確認。

## 10. 第1弾セッション(2026-07-18〜19)の完了条件検証状況

SPEC.md 7章の完了条件のうち、今回のセッション(計算エンジンの土台・会社員のみ・自治体3件のダミーデータ・最低限UI)で検証できた範囲は以下のとおり。次回セッションはこの続きから着手すること。

- **条件4(所得税・住民税が国税庁の計算例と一致)**: 部分達成。
  - 所得税は国税庁「令和7年分 年末調整のしかた」の公式計算例3件と一致確認済み(`test/calc/salary-income.test.mjs` `income-adjustment.test.mjs` `income-tax.test.mjs`、ブラウザ版は `src/calc/test.html`)。
  - 厚生年金保険料は第三者公開記事(30歳・独身・東京都・年収500万円)の数値と完全一致。
  - 住民税は公式計算例が見つからず、自己手計算の検算のみ(上記9章参照)。**総務省等の公式値との照合は次回以降の宿題**。
- **条件7(スマホ375px表示)**: ユーザー側でブラウザ開発者ツールにより確認予定(このセッションでは未実施)。
- **条件10(かんたん入力のみで完結、エラーにならない)**: 達成。子供0人・配偶者なし・年収の極端に低い/高いケース(所得金額調整控除が発動する850万円超帯を含む)を `test/calc/*.test.mjs` で自動テスト化済み。
- **条件2(国保データの未整備を無言で欠落させない)**: 2026-07-19時点で13/1,741(手動収集10件: 市川市・新宿区・札幌市・金沢市・那覇市・福岡市・大阪市・川口市・軽井沢町・檜枝岐村 + 段階3パイロットの自動収集3件: 福島市・仙台市・堺市)。`nationalHealthInsuranceStatus: "confirmed"|"pending"` で明示し無言の欠落なし。また、手動収集10自治体のうち7件(千葉・東京・北海道以外)は協会けんぽ料率(会社員側)が`kyokaiKenpoStatus: "pending"`のまま(未整備)であることが判明し、同様に無言の欠落なくUIで案内する対応を追加した(`src/ui/main.js`の`isEmploymentTypeAvailable`)。
- **条件1(会社員・フリーランス両方の手取り概算)**: 2026-07-21、**完全達成**。計算エンジンに加え、UI上でも雇用形態セグメントからフリーランスを選択でき、国保・国民年金の内訳表示、SPEC.md 3章 派生3(会社員⇔フリーランス比較カード、常時表示)まで実装済み。ユーザーのブラウザ確認済み。
- **条件8(免責事項・プライバシーポリシー・運営者情報・PR表記が全ページから到達可能)**: 2026-07-23、**達成**。`/about/`(運営者情報・広告についてPR表記を含む)・`/privacy/`・`/disclaimer/`の3ページを新設し、`index.html`のヘッダー・フッターから実リンクで到達可能。連絡先メールアドレスは`assets/contact.js`で実行時に組み立てて表示(クローラー対策)。
- **対象外(今回スコープ外につき未実装)**: 条件3(国保±1%照合。1,741件整備後の課題)・条件5(ふるさと納税)・条件6(市区町村別ページ)・条件9(GitHub Actionsは接続済みだがPages公開は未設定)。

### 次回セッションで着手すべきこと(優先度順の目安、2026-07-24時点で更新)
1. ~~GitHub リポジトリとのリモート接続・初回push、GitHub Actions ワークフロー(`node --test`)の追加~~ → **2026-07-20 完了**
2. ~~住民税の調整控除の人的控除差を総務省一次資料で確定~~ → **2026-07-20 一部完了**(配偶者控除・扶養控除等は確定。基礎控除分のみ未確定、上記9章参照)
3. ~~フリーランス側の計算エンジン(国保料+国民年金)の実装~~ → **2026-07-20 完了**
4. ~~UIでフリーランス選択を有効化し、SPEC.md 3章 派生3(会社員⇔フリーランス比較)の表示を実装~~ → **2026-07-21 完了**
5. ~~免責事項・プライバシーポリシー・運営者情報等のページ整備(SPEC.md 7章 条件8)~~ → **2026-07-23 完了**
6. ~~段階3: 都道府県単位の国保料自動収集パイプライン(Python)の設計・実装~~ → **2026-07-19 パイロット(福島・大阪・宮城)完了**。`scripts/kokuho/`にfetch→extract(Claude API)→build→テスト→コミットの一連のパイプラインを実装し、`.github/workflows/kokuho-pipeline.yml`(workflow_dispatch)で実行できる状態にした。福島市・仙台市・堺市の3件が新規に自動登録され、大阪市・檜枝岐村は既存の手動データを上書きせず比較用ファイル(`data/raw/kokuho/comparisons/`)に出力された(想定どおりの動作)。
   2026-07-22のパイロット収集(手動)で得た知見:
   - 都道府県が「市町村標準保険料率」を一覧PDF/Excelで公表しており(例: 福島県 `pref.fukushima.lg.jp/uploaded/attachment/731236.pdf`)、これが最も効率的な一次ソース(SPEC.md 5章が想定する方式そのもの)。ただし「標準保険料率」は市町村の実際の条例料率と一致しない場合がある旨が資料に明記されており、可能なら市町村独自の実際の料率で上書きする2段階設計が望ましい
   - 大阪府のように県内市町村で保険料率を統一している都道府県がある(この場合は都道府県1件のデータで県内全市町村をカバーできる)。ただし2026-07-19のパイロットでは、大阪府は医療分・支援分・介護分は全市町村共通だが子ども・子育て支援納付金分の均等割額に市町村差があったため、Claude APIは`unifiedRate: false`として個別出力した(完全な統一料率ではない都道府県もあることが判明)
   - 収集の優先順位は「県庁所在地→政令指定都市→その他の市町村」とする(ユーザー指示、2026-07-22)
   - 小規模自治体(村等)は独自サイトに料率が掲載されていないことが多く、都道府県公表の一覧が実質的な一次資料になる
   - 2026-07-19のパイプライン実装時に判明した技術的教訓は11.6章を参照。全1,741件への本格展開(市町村コードマスタの整備含む)は次回以降の課題として残っている
7. ~~協会けんぽ料率の追加整備~~ → **2026-07-26 完了**(47都道府県全件。詳細は11.9章参照)
8. 詳細入力モードの各項目(iDeCo・生命保険料・医療費・住宅ローン控除・経費率・青色申告控除等)の実装
9. GitHub Pagesへの公開設定(現状はCIのみでPages公開は未設定。SPEC.md 7章 条件9の後半)。**2026-08-08、robots.txt/sitemap.xml/デプロイ用ワークフローの追加に着手**。詳細・注意点は11.10章参照。リポジトリ設定の「Pages → Source → GitHub Actions」への切り替えは、このクラウド環境のプロキシがGitHub Pages設定APIへのアクセスを拒否するため自動化できず、ユーザーが手動で行う必要がある
10. (余裕があれば)基礎控除の人的控除差をe-Gov法令検索等で確定、配偶者特別控除の所得区分表の令和8年度改正追随確認 ~~、市川市の子ども・子育て支援納付金分の年齢区分(18歳未満/以上どちらが2,000円か)の再確認~~ → **2026-07-25 解決済み(11.7章参照)**
11. ~~国保データ全国展開 フェーズ1: 市町村コードマスタ整備~~ → **2026-07-24 完了**(`docs/国保料データ全国展開_設計方針_2026-07-19.md`参照。総務省「全国地方公共団体コード」(`data/raw/municipalities/000925835.xlsx`)から`scripts/municipalities/build_master.py`で`src/data/municipalities/master.json`(基礎自治体1,747件)・`designated-city-wards.json`(政令指定都市の区、parentCodeで親市参照)を生成)
12. ~~国保データ全国展開 フェーズ2: 人口順の都道府県展開リスト作成~~ → **2026-07-24 完了**(`docs/国保料データ全国展開_設計方針_フェーズ2追記_2026-07-24.md`参照。総務省「住民基本台帳に基づく人口、人口動態及び世帯数」(令和7年1月1日現在、`data/raw/population/001023712.xlsx`)から`scripts/population/build_prefecture_ranking.py`で`scripts/kokuho/prefecture_rollout_order.json`(47都道府県、人口降順rank付き)を生成。既存パイプライン実証済みの福島県・大阪府・宮城県は`pipelineStatus: "verified_pilot"`、残り44県は`not_started`)
13. ~~低所得世帯向け軽減(7割・5割・2割)の実装~~ → **2026-08-07 完了**(詳細は11.8章。加算単価31万円・57万円の一次資料照会は引き続き未確認)
14. 国保データ全国展開 フェーズ3: 自動収集パイプラインのスケール展開。**2026-07-24〜08-08、rank1〜8(東京都・神奈川県・大阪府・愛知県・埼玉県・千葉県・兵庫県・福岡県)が完了**。東京都62市区町村中59件confirmed(3件not_available)、神奈川県33市町村中33件confirmed、大阪府43市町村中43件confirmed(大阪市1件はverified、42件は自動収集)、愛知県54件confirmed、埼玉県63件confirmed、千葉県54件confirmed、兵庫県41件confirmed、福岡県60市町村中60件confirmed(福岡市1件は既存の手動データを`should_write`の優先度制御で上書き保護、残り59件を自動収集。北九州市のperCapitaAmountOver18=1,137円〔均等割1,090円+18歳以上均等割47円〕を一次資料PDFと照合し完全一致を確認済み)。次はrank9(北海道)以降。
   - **コスト最適化(2026-07-25)**: PDFのテキスト化(base64画像送信→事前テキスト抽出)は、count_tokensで実測するとPDF文書ブロック送信の方が約83%多くトークンを消費すると判明したが、複雑な多列表をpypdfで単純テキスト抽出すると列の対応関係が崩れ抽出精度を損なうリスクが高いため見送った。プロンプトキャッシュ(`extract.py`のPDF文書ブロックに`cache_control: {type: "ephemeral"}`)は採用し、同一都道府県内の複数バッチ呼び出しで2回目以降がキャッシュヒットすることを実行ログで確認済み。Batch API(50%引き)は非同期化が必要で対話的なデバッグサイクルと相性が悪いため保留。
   - **build.pyの比較用ファイル命名バグ(2026-07-25発見・修正)**: `should_write()`がFalseを返した際の比較用ファイル名が`{muni_code}_{source_id}.json`固定だったため、単一ソース都道府県(source_id="default")では別名ファイルが作られ、既存の正しい比較用ファイルが更新されない不整合があった。`comparison_path_for()`を追加し、`raw_path_for`/`extracted_path_for`と同じ命名規則(単一ソースは接尾辞なし)に統一した。本番データへの実害はなかった。
    - **東京都対応で判明した重要な知見(2026-07-24)**:
      - 一部都道府県の標準保険料率PDFは「①都道府県標準(単一値)・②市町村標準(2方式仮定の理論値)・③市町村標準(各市町村の実際の算定方式)」という3階層構造を持つ(東京都・神奈川県で確認。福島県・宮城県は単一階層、大阪府は真の統一料率のため該当なし)。**必ず③の列を対象にするよう`prefectures.json`の`extractionNote`で明示すること**。指定を忘れると②(仮定値)が混入する
      - 一部の市町村(東京都の島嶼部の一部)は資産割(固定資産評価額に応じた賦課)を含む4方式。`scripts/kokuho/`のスキーマに`assetRate`フィールドを追加済み(2026-07-24)。0=該当区分なし、null=読み取り不能、の2値運用
      - 極小人口自治体では標準保険料率の理論値がマイナスになることがある(東京都: 利島村・御蔵島村・青ヶ島村)。`build.py`の`RATE_RANGE`検証で自動的に弾かれ、`src/data/municipalities/index.json`に`nationalHealthInsuranceStatus: "not_available"`として理由付きで明示される(`update_municipalities_index_not_available()`、2026-07-24追加、都道府県固有ではない汎用機能)
      - **事故の教訓**: 1つの都道府県が複数の一次資料ソースを持つ場合(例: 東京都の特別区=実際の適用値 と それ以外=標準保険料率)、同じ市町村コードが両方のソースの一覧表に登場しうる。dataSourceの種類を区別しない上書き保護だと、信頼度の高いデータが低いデータで上書きされる事故が起きる(実際に千代田区等22区で発生し、復旧した)。`build.py`に`DATA_SOURCE_PRIORITY`による優先度付き上書き制御(`should_write()`)を導入済み。新しい都道府県を追加する際、一次資料に想定外の市町村(隣接カテゴリの行)が紛れ込んでいないか要確認
      - Claude Sonnet 5モデルは応答にthinkingブロックを含み、複雑な表(市町村数が多い・階層が複雑)ではthinkingだけでmax_tokensを消費し尽くすことがある(福島県の「応答途中切れ」問題とは別の失敗モード)。`extract.py`は`MAX_TOKENS=32000`・`BATCH_SIZE=10`・`messages.stream()`(create()だと10分超過時にSDKがエラーを返すため)に対応済み

## 11.5 CI(GitHub Actions)に関する教訓(2026-07-19)

Test #7〜Test #14(約8回分)のあいだ、CI(`test.yml`の`node --test`ステップ)が
壊れたまま気づかれず、その間に「CI成功を確認した」という誤った報告を複数回
行ってしまった。原因と再発防止策を以下に記録する。

- **原因**: `.github/workflows/test.yml` で `node --test test/calc/` のように
  ディレクトリパスを明示引数として渡すと、Node.jsの`--test`はディレクトリを
  再帰探索するのではなく、末尾スラッシュが失われた状態でモジュール解決
  (import相当)を試み、`Error: Cannot find module '.../test/calc'`で失敗する。
  対応: 明示パス指定をやめ、`node --test`(引数なし)に変更した。既定の自動探索は
  「`test`という名前のディレクトリ配下」「`*.test.mjs`等の命名規則」を再帰的に
  検出する仕様であり、`test/calc/*.test.mjs`は両条件を満たすため確実に拾われる。
- **教訓1**: ローカルのブラウザテスト(`src/calc/test.html`)とCI(`node --test`)は
  別物であり、どちらか一方が成功していても、もう一方が壊れている可能性がある。
  今後「CI成功」と報告する際は、run number・実行時間を明記すること。実行時間が
  10秒未満など極端に短い場合は、テストが実際には実行されずに早期失敗している
  疑いがあるため、成功と決めつけないこと。
- **教訓2**: GitHub ActionsのHTMLページ(`https://github.com/.../actions`)は
  JavaScriptで描画されるSPAであり、WebFetchでは正確に読み取れず、実際には
  失敗しているのに「成功」と要約されることがあった(このセッションで実際に発生)。
  CI結果の確認には、GitHub REST API(`api.github.com/repos/.../actions/runs`の
  `conclusion`フィールド)を使うか、ユーザーにブラウザでの目視確認を依頼すること。
  ジョブの生ログ本文(`.../actions/jobs/{id}/logs`)は認証が必要で取得できない
  (403)ため、詳細なエラーメッセージが必要な場合はユーザーに貼り付けを依頼する。

## 11.6 kokuho自動収集パイプライン(scripts/kokuho/)構築時の教訓(2026-07-19)

段階3パイロット(福島・大阪・宮城)の実装からworkflow_dispatch成功までに、
Claude APIを使った抽出処理(`scripts/kokuho/extract.py`)で4回連続して
問題が発生し、都度ログを確認しながら修正した。再発防止のため記録する。

1. **JSON構文エラー**(`Expecting ',' delimiter`): プロンプトが「JSON以外
   出力しない」ことを明示していても、値の直後に補足説明を書き足す等で
   不正なJSONが返ることがある。対応: プロンプトにコメント・末尾カンマ・
   値への補足説明を明示的に禁止する項目を追加し、パース失敗時は生の
   レスポンステキストをログとファイルの両方に必ず残すようにした
   (`parse_json_response`)。
2. **応答が空文字列**(`Expecting value: line 1 column 1`): API呼び出し
   自体は例外を送出せず成功していたが、`message.content`に`text`タイプの
   ブロックが含まれていなかった。対応: レスポンス受信直後に必ず
   `stop_reason`と`content_block_types`をログ出力し、textが空の場合は
   その場で具体的な内容のエラーを出すようにした(`call_claude`)。
3. **max_tokens到達による応答の途中切れ**: 市町村数が多い都道府県
   (福島県59件)で、1回のAPI呼び出しに全市町村を含めようとすると応答が
   途中で切れた。max_tokensの引き上げ(8192→16384)だけでは根本解決に
   ならない(将来もっと市町村数の多い都道府県で再発する)と判断し、
   「まず市町村名一覧だけ取得→15件ずつのバッチで個別に抽出」という
   2段階方式に設計変更した(`extract.py`の`NAME_LIST_PROMPT`/
   `batch_extraction_prompt`)。
4. **プロンプトの自己矛盾によるバッチ間の不整合**: 上記2段階化の直後、
   大阪府だけ目的の市町村が0件になった。原因はプロンプト内に
   「平等割額は列が無ければ0」と「表に存在しない数値はnullに」という
   矛盾する指示が同居しており、バッチ(=別々のAPI呼び出し)ごとに
   モデルの解釈が割れ、`build.py`の検証(数値必須)に一部だけ弾かれて
   いたこと。**教訓**: バッチ分割方式では、各バッチが完全に独立した
   文脈でAPIを呼び出すため、プロンプトの曖昧さがバッチ間の不整合として
   顕在化しやすい。プロンプトに矛盾する指示が無いか、分割前よりも
   注意深く確認する必要がある。
5. **総括**: ワークフローが green(conclusion: success)であることと、
   生成されたデータが意図したとおりの中身であることは別問題。
   `build.py`は「検証エラーの市町村を静かにスキップする」設計
   (SPEC.md 5章の「推測で埋めない」方針に沿った意図的な挙動)のため、
   目的の市町村が実際に生成されたか(件数・中身)を都度
   `git show <pipeline commit> --stat`等で確認する習慣が必要。

## 11.9 協会けんぽ47都道府県分の料率整備(2026-07-26)

トップページに「都道府県別の社会保険料率の違い」を解説するセクションを追加した
延長で、協会けんぽ料率が北海道・東京都・千葉県の3件しか整備されていない実態が
判明し、残り44都道府県分を整備した。

- **重要な発見**: 既存3件のデータを分析した結果、都道府県で異なるのは「一般保険料率
  (healthInsuranceRate)」という1つの数値だけだと判明した。50等級の標準報酬月額区分
  (等級・上下限・標準報酬月額)は健康保険法上全国共通、介護保険料率(1.62%)・
  子ども子育て支援金率(0.23%)も協会けんぽ全体で一律である。そのため、各都道府県の
  個別PDFを47件収集する必要はなく、「都道府県毎の一般保険料率一覧」という1つの
  一次資料(協会けんぽ公式サイト、令和8年度)から47都道府県分の料率(%)さえ取得できれば、
  50等級分の金額表はプログラムで機械的に算出できる。この一次資料の値は既存3件
  (北海道10.28%・東京都9.85%・千葉県9.73%)と完全一致し、ソースの正しさを検証済み。
- **実装**: `scripts/kyokai-kenpo/build.py`を新規作成。47都道府県の料率をハードコードした
  定数テーブルとして持ち、既存`tokyo.json`の等級構造をテンプレートに全都道府県分の
  JSONを生成し、`src/data/municipalities/index.json`の該当市区町村の`kyokaiKenpoStatus`を
  `confirmed`に更新する(当時148市区町村中145件が新規confirmed化)。既存3件も再生成したが、
  数値が完全一致することを別途検証スクリプトで確認済み(出典URL・取得日等のメタデータのみ
  更新)。
- **今後の教訓**: 都道府県別データを整備する際、まず「本当に都道府県ごとに全項目が
  異なるのか、それとも一部の数値だけが違うのか」を既存データの逆算で確認する価値がある。
  国保料(kokuho)は市区町村ごとに料率・均等割・平等割など複数項目が独立に異なるため
  47都道府県×多数市区町村の個別収集(scripts/kokuho/)が必要だったが、協会けんぽは
  法定の等級表が全国共通のため、はるかに軽量な整備で済んだ。両者を同じ手法(PDF個別収集)
  で整備しようとしていたら無駄な工数がかかっていた。

## 11.10 GitHub Pages公開設定に関する教訓(2026-08-08)

SPEC.md 7章条件9(GitHub Pages公開)に着手した際、robots.txt・sitemap.xmlの実効性について
誤った前提で作業を始めかけたため、判明した事実と対応方針を記録する。

- **robots.txtはこのリポジトリに置いても効果が無い**: GitHub Pagesのproject
  page(`https://norinori197924-hub.github.io/tedori-sim/`のようなサブパス配信)では、
  クローラーは`https://norinori197924-hub.github.io/robots.txt`(ルートドメイン)しか見ない。
  `/tedori-sim/robots.txt`は読まれない。ルートドメインの`robots.txt`は
  `norinori197924-hub.github.io`という別リポジトリ(user/organization pages)の管轄であり、
  このリポジトリ単体では制御できない。そのため**robots.txtファイルの作成は見送った**。
  クロール除外が必要なページには、代わりにページ単位で`<meta name="robots" content="noindex">`を
  `<head>`に追加する方式にした(`src/calc/test.html`に適用済み)。scripts/・CLAUDE.md・SPEC.md・
  docs/・data/raw/等、HTMLページではないファイルにはこの方式が使えないため、現状は
  「サイトのナビゲーションからリンクしない」ことのみが実質的な対策になっている(2026-08-07
  セッションの調査で確認済み。実害は小さいと判断)。
- **sitemap.xmlも同じ理由で自動的には効かない**: サブパス配信ではsitemapの自動検出(通常は
  ルートドメインのrobots.txtに`Sitemap:`行を書いて知らせる方式)が使えない。ファイル自体
  (`/sitemap.xml`、`/`・`/about/`・`/privacy/`・`/disclaimer/`の4件)は作成済みだが、
  検索エンジンに登録するには**Google Search Console等で手動送信が必要**。公開後の作業として
  残る(このセッションでは未実施)。
- **Google Search Console 所有権確認ファイル(2026-08-08追加)**: リポジトリルートに
  `google29e95ef41fb28712.html`(内容は`google-site-verification: google29e95ef41fb28712.html`の
  1行のみ)を設置済み。`https://norinori197924-hub.github.io/tedori-sim/google29e95ef41fb28712.html`
  でSearch Consoleが所有権を確認するためのファイルであり、**削除すると所有権の確認が外れる
  ため、削除・リネームしないこと**。sitemap.xmlの手動送信(上記)はこのファイルによる
  所有権確認が前提になるため、まだ完了していない場合は先にSearch Console側での確認作業を
  済ませること。
- **独自ドメイン等でrobots.txtが将来有効になった場合のための正しいパスのメモ**:
  リポジトリには紛らわしい2つの「data」ディレクトリがある。
  - `data/raw/`(リポジトリ直下): 国保料収集パイプラインの生データ・中間ファイル
    (PDF抽出結果、都道府県別の比較用ファイル等)。サイトの実行時には一切参照されない。
  - `src/data/`: 計算エンジンが実行時に`fetch()`する本番データ(料率JSON・市区町村マスタ等)。
    現在のトップページはこれをブラウザ側JSで読み込んで計算結果を表示している。
  **将来robots.txtが実際に有効になる構成(独自ドメイン化等)に移行する場合**、`data/raw/`を
  `Disallow`にするのは問題ないが、`src/data/`を安易に`Disallow`にすると、Googlebotが
  レンダリング時にこのデータを取得できず、クライアントサイドでデータを描画する現行方式の
  ページ(将来の`/city/`ページ含む)の内容を正しく評価できなくなるおそれがある(Googleは
  CSS/JS同様、ページ描画に必要なリソースのブロックを推奨していない)。一方で、`src/data/`配下の
  生JSONファイルが検索結果に直接ヒットする「薄いコンテンツ」問題を避けたい場合は、あえて
  `Disallow`にする選択肢もある。どちらを取るかは、将来`/city/`ページをビルド時に静的HTMLへ
  数値を焼き込む設計にするか、現行同様クライアントサイドfetchのままにするかによって最適解が
  変わるため、**`/city/`ページの実装方式を決めるタイミングで再検討すること**。

## 11. 2026-07-22に発見した国保計算の未確定事項

- **未就学児(6歳未満)の均等割5割軽減**(全国一律制度、令和4年度〜)は`national-health-insurance.js`に実装済み(医療分・支援分の均等割のみ対象、介護分は対象外)。~~ただし子ども・子育て支援納付金分(令和8年度新設)への適用有無は自治体資料から確認できておらず、未適用のまま~~ → **2026-07-25、下記11.7章のとおり解明・解決済み**(未就学児限定の軽減ではなく、18歳未満全員が一律0円という別ルールだった)。令和9年度からは医療分・支援分の5割軽減の対象が高校生まで拡大予定(2027年4月施行、今回の対象年度=令和8年度にはまだ適用されない)。
- ~~子ども・子育て支援納付金分の均等割について、「18歳未満/以上」の内訳が資料から一意に確定できなかった自治体がある(福岡市・檜枝岐村)~~ → **2026-07-25、下記11.7章のとおり解決済み**。

## 11.7 子ども・子育て支援納付金分の「18歳未満均等割0円」ルール発見(2026-07-25)

「未就学児に係る均等割額の軽減措置」の実装依頼を受けて調査した結果、当初の想定
(未就学児限定の5割軽減を子ども・子育て支援納付金分にも適用する)とは異なる、
より根本的な事実が判明した。

- **確定した事実**: 子ども・子育て支援納付金分の均等割は、**18歳未満(年齢を問わず、
  未就学児に限らない。小学生・中学生・高校生年代も含む)が全国一律で全額軽減(0円)
  される法定制度**(国民健康保険法施行令等に基づく)。軽減で生じる財源は、18歳以上の
  被保険者への「18歳以上均等割額」という追加負担で賄う設計。こども家庭庁「子ども・
  子育て支援金制度について」(令和7年3月、PDF)15ページの図解、および福島市・
  仙台市・大阪市の公式ページで確認済み。
- **判明した実装上の帰結**: 都道府県公表の「標準保険料率」PDFに載っている「均等割額」
  列は、実際には18歳以上の被保険者にのみ課される基礎額であり、「18歳以上均等割額」
  列(加算額)と合算した金額が18歳以上1人あたりの実際の負担額になる。この事実に
  気づかず、自動収集パイプラインは「均等割額」列の値をそのまま`perCapitaAmountUnder18`
  に格納していたため、本来0円であるべき値が非ゼロになっていた(自動収集分135件+
  手動収集分5件〔檜枝岐村・市川市・軽井沢町・大阪市・福岡市〕、計140件を修正、
  コミット`2ea0adf`)。市川市(12203)のみ、上記とは独立した別バグ(公式ページの
  見出し「子ども分〔18歳未満を除く〕」の読み違いによる合算漏れ)があり、
  `perCapitaAmountOver18`も2,000円→2,100円に修正した。
- **未就学児軽減の実装は不要と判明**: 子ども・子育て支援納付金分には「未就学児だけ
  5割軽減」という制度はそもそも存在しない。`national-health-insurance.js`の
  計算式(`childSupportSection.perCapitaAmountUnder18 * childSupportUnder18Count`)は
  既存のまま変更不要で、データ側を0円に修正するだけで制度上の要件を満たす。
  医療分・支援分の未就学児5割軽減(既存実装)とは別制度であり、互いに影響しない。
- **今後の教訓**: 都道府県公表資料の「均等割額+18歳以上均等割額」という2列構成は、
  一見「基礎額+加算額」に見えるが、実際は**両方とも18歳以上専用**というケースが
  ある(今回確認できた範囲では全国共通)。今後さらに都道府県を展開する際、
  同じ列構成のPDFが出てきたら、`extractionNote`または`build.py`側で
  `perCapitaAmountUnder18`を機械的に0にする設計を標準にすることを検討する余地がある。
- **手計算テストが古いデータ前提のまま固定値になっていた教訓**: 上記データ修正
  (コミット`2ea0adf`)をpushしたところ、CI(Test #29・#30)で57件中2件が失敗した。
  原因は`test/calc/national-health-insurance.test.mjs`の手計算設例A・Bが市川市
  (12203)を使っており、テストの期待値が旧データ(修正前のchildSupport)を前提にした
  固定値のままだったこと。ユーザーがCIログの「not ok」行から発見し、期待値を
  修正後のデータと計算式から独立に手計算し直して解決した(コミット`9f00440`、
  Test #31で成功)。**教訓**: 特定の市区町村コードをハードコードした手計算テストは、
  その自治体のデータファイルを後から修正すると、テスト自体も連動して見直しが
  必要になる。データ修正時は、その市区町村コードを参照しているテストファイルを
  grep等で洗い出し、影響有無を都度確認する習慣が要る(`engine.test.mjs`のように
  絶対値ではなく相対検証のみのテストは影響を受けないため、この確認は「絶対値を
  検証しているテストか」で絞り込むと効率的)。

## 11.8 低所得世帯向け軽減(7割・5割・2割)は実装済み(2026-08-07)

2026-07-25に未実装であることを確認していた国保料の低所得世帯向け軽減(所得に応じた
7割・5割・2割の均等割・平等割軽減)を、2026-08-07に`src/calc/national-health-insurance.js`
に実装した(ブランチ`feature/kokuho-low-income-reduction`)。

### 設計の要点

- 軽減判定基準額(基礎控除相当額43万円、被保険者数に乗じる額31万円/57万円、
  給与所得者等の加算額10万円)は、地方税法施行令に基づく全国一律の値として
  計算エンジン内の定数(`LOW_INCOME_REDUCTION_COEFFICIENTS_2026`)で1箇所に保持し、
  自治体データ側(`national-health-insurance/*.json`)には持たせない設計とした。
  これにより、既存の自治体データ(2026-08-07時点で確定〔`nationalHealthInsuranceStatus: "confirmed"`〕355件、
  理論値マイナスによる`not_available`3件を含めた登録済み358件)の再抽出は不要と判断した
  (2026-08-08、実データをgrepで再検証して修正。旧版はここに「523件」という、実測に基づかない
  誤った数字を記載していた)。
- **一次資料未確認事項**: 加算単価(31万円・57万円)は令和8年度の値として複数自治体の
  公表資料・厚労省関連資料等からの調査(このCLAUDE.mdの旧版に記載していた調査結果)に
  基づく採用値であり、地方税法施行令原文への直接照会(e-Gov法令検索等)は実施できて
  いない(ネットワーク制約でPDF・JavaScript描画ページの直接取得ができなかったため。
  9章の基礎控除人的控除差と同種の限界)。次回、ブラウザ操作が可能な環境で条文を
  確認するか、市販の地方税コンメンタールで裏取りすること。
- 適用範囲: 均等割・平等割のみ(所得割は対象外)。医療分・支援分・介護分・子ども
  子育て支援納付金分すべてに適用する。
- 適用順序: 低所得軽減(`truncateYen`で軽減後の均等割・平等割を算出)→ 未就学児5割軽減
  (軽減後の均等割にさらに`floor(×0.5)`を適用)の順。
- 軽減判定所得は、本人の合計所得金額(事業所得)に配偶者の合計所得金額(配偶者控除の
  判定と同じ方法で、入力年収に給与所得控除を適用して推定)を合算して求める。43万円は
  差し引かない(基準額側に含まれる)。

### 実装上の制約(SPEC.mdにも記載)

- **特定同一世帯所属者は当面0人として扱う**(未対応)。
- **給与所得者等の判定の簡易化**: このアプリの入力モデルでは雇用形態や職業を持つのは
  本人(常にフリーランス)のみで、配偶者・子供は年収(または年齢)しか入力されない。
  そのため、本人は常に給与所得者等に非該当(事業所得のため)、配偶者は入力年収(額面)が
  55万円を超える場合のみ該当、子供は常に非該当として判定する。この結果、給与所得者等の
  数は最大1人(配偶者のみ)にしかならず、加算額の算式`100,000×(給与所得者等の数-1)`は
  このアプリの現在の入力モデルでは常に0円になる(将来、詳細入力モードで世帯員の
  雇用形態が入力可能になった場合に効いてくる、法令どおりの実装)。
- 軽減判定所得の算出そのもの(配偶者の所得を合算する部分)は制約の影響を受けず、
  常に正しく機能する。

### テスト

`test/calc/national-health-insurance.test.mjs`に17件の手計算設例テストを追加した
(既存6件と合わせて計23件)。内訳: 7割/5割/2割×未就学児あり/なしの基本6件、配偶者所得の
合算確認1件、境界値(430,000円/430,001円/740,000円/740,001円/1,000,000円/1,000,001円)
6件、被保険者数が基準額に反映されることの確認2件、軽減乗数の端数処理順序(truncateYenと
floorの二重適用)の確認1件、平等割・介護分への軽減適用確認(札幌市)1件。既存の軽減
非該当ケース(年収500万円・600万円等)は`lowIncomeReduction.level === 'none'`かつ
数値が従来と完全一致することを確認済み。
